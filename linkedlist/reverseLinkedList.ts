/**
 * Reverse a linkedList
 **/

import ListNode from "./list";

function reverse(head: ListNode) {
  let currentNode = new ListNode(head);
  while (currentNode) {
    let nextNode = new ListNode(currentNode);
  }
}
